{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "golangci-lint run ./...",
    "test": "go test ./...",
    "build": "sh -c 'mkdir -p build && go build -buildmode=plugin -o build/match_handler.so ./modules'",
    "format": "go fmt ./...",
    "typecheck": "go vet ./..."
  }
}
