version: '2'

run:
  timeout: 5m
  modules-download-mode: readonly # Don't download missing modules

issues:
  max-issues-per-linter: 50
  max-same-issues: 10

linters:
  exclusions:
    paths:
      - vendor/
      - node_modules/
      - .git/
      - ".*\\.pb\\.go$"
      - ".*_generated\\.go$"
