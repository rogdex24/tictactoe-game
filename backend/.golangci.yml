version: 2

run:
  timeout: 5m
  modules-download-mode: readonly # Don't download missing modules

issues:
  max-issues-per-linter: 50
  max-same-issues: 10
  exclude-dirs:
    - vendor
    - node_modules
    - .git
  exclude-files:
    - ".*\\.pb\\.go$"
    - ".*_generated\\.go$"
